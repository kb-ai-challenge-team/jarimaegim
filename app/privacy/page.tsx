"use client";
import { useState } from "react";
import Link from "next/link";
import { ArrowLeft, Bell, Database, FileText, LoaderCircle, Shield, Trash2 } from "lucide-react";
import { api, ApiError } from "@/lib/api";
export default function PrivacyPage(){const [status,setStatus]=useState("");const [busy,setBusy]=useState(false);async function request(){setBusy(true);try{const r=await api.createPrivacyRequest("ERASE");setStatus(`삭제 요청이 접수되었습니다. 처리 번호: ${r.request_id}`);}catch(e){setStatus(e instanceof ApiError?e.message:"요청을 접수하지 못했습니다.");}finally{setBusy(false)}}return <main id="main-content" className="privacy-page"><header><Link href="/" className="back-link"><ArrowLeft/> 터닥터</Link><h1>내 데이터와 개인정보</h1><p>서비스가 보유하는 정보와 동의 상태를 확인하고 삭제를 요청할 수 있습니다.</p></header><div className="privacy-grid"><section><Database/><div><h2>창업 케이스</h2><p>확정한 조건, 후보, 분석 결과는 케이스 소유자만 접근합니다.</p><span>익명 세션: 최대 24시간</span></div></section><section><FileText/><div><h2>생성 문서</h2><p>PDF는 비공개 저장소에 보관하고 만료되는 주소로만 내려받습니다.</p><span>로그인 후 사용 가능</span></div></section><section><Bell/><div><h2>알림 설정</h2><p>인앱 알림이 기본이며 이메일은 종류별로 직접 선택합니다.</p><span>마케팅 알림 없음</span></div></section><section><Shield/><div><h2>AI 처리</h2><p>AI는 설명을 돕고 점수·비용·자격을 직접 계산하지 않습니다.</p><span>OpenAI store:false</span></div></section></div><section className="danger-zone"><div><Trash2/><div><h2>내 데이터 삭제 요청</h2><p>DB, 문서, 알림, 캐시와 백업 롤오프 추적을 포함한 삭제 작업을 시작합니다.</p></div></div><button onClick={request} disabled={busy}>{busy?<LoaderCircle className="spin"/>:"삭제 요청 접수"}</button>{status&&<p role="status">{status}</p>}</section><footer>개인정보 권리 요청 전용 이메일: <strong>{process.env.NEXT_PUBLIC_PRIVACY_RIGHTS_EMAIL||"운영 도메인 확정 후 표시"}</strong></footer></main>}
