"use client";
import { useState } from "react";
import Link from "next/link";
import { ArrowLeft, Database, FileText, LoaderCircle, Shield, Trash2 } from "lucide-react";
import { api, ApiError } from "@/lib/api";
export default function PrivacyPage(){const [status,setStatus]=useState("");const [busy,setBusy]=useState(false);async function request(){setBusy(true);try{const r=await api.createPrivacyRequest("ERASE");setStatus(`삭제 요청 번호가 발급되었습니다: ${r.request_id}`);}catch(e){setStatus(e instanceof ApiError?e.message:"요청 번호를 발급하지 못했습니다.");}finally{setBusy(false)}}return <main id="main-content" className="privacy-page"><header><Link href="/" className="back-link"><ArrowLeft/> 자리매김</Link><h1>내 데이터와 개인정보</h1><p>현재 익명 세션에 연결된 정보의 접근 기간과 처리 원칙을 확인할 수 있습니다.</p></header><div className="privacy-grid"><section><Database/><div><h2>창업 케이스</h2><p>확정한 조건, 후보와 분석 결과는 현재 브라우저의 유효한 익명 세션에서만 접근합니다.</p><span>접근 유효기간: 최대 24시간</span></div></section><section><FileText/><div><h2>생성 문서</h2><p>PDF는 비공개 저장소에 보관하며 문서를 만든 익명 세션에서만 내려받을 수 있습니다.</p><span>세션 만료 후 접근 불가</span></div></section><section><Shield/><div><h2>AI 처리</h2><p>AI는 설명을 돕고 점수·비용·자격을 직접 계산하지 않습니다.</p><span>OpenAI store:false</span></div></section></div><section className="danger-zone"><div><Trash2/><div><h2>데이터 삭제 요청</h2><p>세션 만료는 브라우저 접근 종료를 뜻하며 서버 삭제 시점과는 다릅니다. 자동 삭제가 연결되기 전에는 이 화면에서 요청 번호만 발급하므로, 실제 권리 행사는 아래 전용 이메일로 접수해 주세요.</p></div></div><button onClick={request} disabled={busy}>{busy?<LoaderCircle className="spin"/>:"삭제 요청 번호 발급"}</button>{status&&<p role="status">{status}</p>}</section><footer>개인정보 권리 요청 전용 이메일: <strong>{process.env.NEXT_PUBLIC_PRIVACY_RIGHTS_EMAIL||"운영 도메인 확정 후 표시"}</strong></footer></main>}
