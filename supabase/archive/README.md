# Archived migrations

`202607190001_initial_baseline.sql` is retained for design history only. It is incompatible with the current FastAPI repository contract (notably anonymous token storage and case input field names) and must not be applied.

The active production baseline is `../migrations/202607200001_initial.sql`, applied to Supabase on 2026-07-22.
