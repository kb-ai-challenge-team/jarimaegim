from __future__ import annotations

# 서울 25개 자치구. 부록 A 불변조건 6의 스코프이며, 서버에서 이 집합 밖은 전부 거절한다.
# main.py·chat_tools.py·condition_interpret.py 세 곳이 같은 판단을 하므로 출처를 하나로 둔다.
#
# 순서는 장식이 아니다: condition_parse.py 의 _district() 는 이 목록을 순회하며 first-match-wins
# 로 자치구를 고른다. 두 자치구가 함께 언급된 문장("강남구에서 하다가 마포구로 이전")에서 어느
# 쪽이 이기는지는 이 순서가 결정한다. 튜플이 아니라 frozenset 이던 시절엔 문자열 해시가 프로세스마다
# 랜덤 시드를 받는 탓에 순회 순서가 실행마다 달라져 같은 문장이 워커에 따라 다른 구로 풀렸다.
# lib/constants.ts 의 SEOUL_DISTRICTS 와 같은 정렬(자치구 코드 순)을 그대로 따른다 — 두 구현이
# 같은 문장에서 같은 답을 내야 하기 때문이다. 튜플도 frozenset 과 마찬가지로 불변이므로, 여러
# 모듈이 같은 객체를 공유해도 안전하다는 원래의 설계 의도는 그대로 유지된다.
SEOUL_DISTRICTS = (
    "종로구", "중구", "용산구", "성동구", "광진구", "동대문구", "중랑구", "성북구", "강북구",
    "도봉구", "노원구", "은평구", "서대문구", "마포구", "양천구", "강서구", "구로구", "금천구",
    "영등포구", "동작구", "관악구", "서초구", "강남구", "송파구", "강동구",
)
